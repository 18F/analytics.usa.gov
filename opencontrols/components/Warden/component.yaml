documentation_complete: false
name: Warden
satisfies:
- control_key: AC-4 (21)
  covered_by: []
  implementation_status: none
  narrative: 'Warden ALLOW rules: Any Warden Server configuration `allow` rules. Set
    Warden Server configuration rules in the Droplet Execution Agent (DEA) configuration
    section of your deployment manifest.

    Warden DENY rules: Any Warden Server configuration `deny` rules. Set Warden Server
    configuration rules in the DEA configuration section of your deployment manifest.

    '
  standard_key: NIST-800-53
schema_version: 2.0
